---
name: ad-review-loop
description: 对抗式审查循环（宿主无关版）。当用户要求"反复审查直到干净""review 到收敛""自动审查并修复""迭代式对抗审查""loop review""re-review until clean""iterative review"，或希望自动循环执行 审查→修复→验证 时使用。适用对象为代码变更、diff、分支。注意：这是会修改代码的迭代版本——若用户只要一份审查报告、不要求修复与重审，改用只读的对抗式审查 skill。
license: MIT
---

# 对抗式审查循环

对代码变更执行**迭代式**对抗审查：每轮由独立上下文子代理审查、修复、验证，直到收敛或触发硬停止。所有子代理必须在独立上下文中运行。

本 skill **可修改工作区文件**（仅限修复阶段），但**绝不 commit、绝不 push**。

## 设计取向

1. **每轮都是 fresh 子代理，这是特性不是妥协。** 上一轮的审查者绝不复用——它已带着既定结论，会形成确认偏误。这也让本 skill **不依赖宿主具备"续跑子代理"能力**，因此比两轮辩论式审查更通用。
2. **状态落盘，不靠记忆。** 收敛判定读状态文件，不依赖编排者的对话上下文。长循环 + 上下文压缩 = 收敛判定静默失效，这是本 skill 最危险的失败模式。
3. **权限按角色分离。** 审查者只读，修复者可写，两者是不同角色、不同权限，不可由同一子代理兼任。
4. **修复者的完成声明不可信。** 与审查者的发现一样，修复者的"我修好了"是**待验证主张**，必须独立验证，含糊即判未修复。
5. **判定必须确定性。** 收敛判据由 `scripts/loop-state.mjs` 计算，不由编排者"感觉差不多了"决定。

## 不变量

1. **强制独立上下文 subagent，每轮 fresh 派生，无静默降级。** 宿主不具备派生能力时停止并告知用户。**禁止继承式派生路径**（Claude Code `/subtask`、`/fork`；DSH `fork` provider）——它们会让子代理继承编排者或上一轮的倾向。
2. **审查者与修复者必须分离。** 审查者只读；修复者只做最小改动。同一个子代理不得既发现又修复（自查偏差）。
3. **状态必须落盘并每轮重读。** 收敛判定只依据状态文件与当前 diff。
4. **绝不 commit / push / 改历史。** 修复只改工作区。
5. **收敛判定必须确定性且显式。** 争议项绝不静默合并；未收敛就如实报告未收敛。
6. **修复完成声明需独立验证。** 含糊即判 `unfixed`（刻意的非对称）。

---

## 第 0 步：能力探测、基线与预算

### 0.1 能力探测（**先做，失败即停**）

| 探测项 | 要确认什么 | 缺失后果 |
|---|---|---|
| **派生能力** | 能否派生**独立上下文**子代理 | **立即停止**，本 skill 无法执行 |
| **只读强制** | 能否用工具白名单/沙箱限制子代理为只读 | 降级为提示词约束，报告中标注 |
| **可写子代理** | 修复者能否获得写权限（且与审查者不同角色） | 退化为「只报告不修复」模式并声明 |
| **状态落盘** | 能否写文件，以及能否运行 `node scripts/loop-state.mjs` | 见下方「内核不可用时的降级」 |

**派生凭据（强制）**：每个子代理必须产出一条可核验凭据：`role`、`agentId`、`provider`、`fresh`、`lens`。**无凭据的子代理视为未执行**，不计入覆盖，且不得写入 `receipts`。

### 0.2 确定预算（**必须先算，不可省**）

开跑前明确写出四个数字：

| 预算项 | 默认值 | 说明 |
|---|---|---|
| `maxRounds` | **3** | 硬上限。**不要依赖"自然收敛"作为唯一出口**——无硬上限的循环在按 token 计费的宿主上是危险的 |
| 每轮审查者数 | 3（局部）/ 5（高风险：并发、状态机、生命周期、核心路径） | 每视角 1 个 |
| 每轮其他子代理 | 1 修复者 + 1 验证者 | 验证者可按文件分组，但不得省 |
| 总子代理预算 | `maxRounds × (审查者数 + 2)` | 默认 3 × 5 = **15** |

**峰值并发自检**：单轮峰值 = 审查者数 + 1 + 1。默认 5 视角时峰值 **7**。

- Claude Code：每 Session ≤200 子代理、默认并发 20 → ✅ 安全
- DSH：每 owner 默认 10 并发，**满容量直接拒绝，不排队** → ✅ 安全（7 < 10），但**不要在同一 owner 上并行跑第二个 loop**
- Codex：`agents.max_concurrent_threads_per_session` → 开跑前确认

**若算出来超过宿主上限**：降低视角数，**不要指望宿主排队**。

### 0.3 建立基线

```bash
mkdir -p .ad-review-loop
node scripts/loop-state.mjs init .ad-review-loop/<对象标识>-state.json \
  --target <分支名或对象标识> --baseline <commit 短 hash> \
  --max-rounds 3 --max-subagents 15
```

明确"不做什么"：不 commit、不 push、不改基线范围外的文件。

---

## 第 1 步：循环体

每轮严格按 1.1 → 1.5 顺序执行，**每轮开始前重读状态文件**。

### 1.1 审查（fresh，只读）

1. 取**当前**完整 diff（不是上一轮的）。
2. 派生 N 个 fresh 只读审查者，各分配**一个**视角，并行。每个收到：完整 diff、分配视角、证据格式要求、派生凭据要求、"先陈述我理解的范围，再给发现"。
3. **所有审查者互不可见**，且**不告知上一轮的任何结论**——包括已修复项。fresh 的含义是彻底 fresh。
4. 每条发现必须带可点击位置：`<文件路径>:<起始行>-<结束行>`。**无位置的发现直接驳回**（内核会在记录阶段丢弃并留痕）。

推荐视角（可适配，新增记入状态）：

| # | 视角 | 关注点 |
|---|---|---|
| 1 | 正确性与回归 | 逻辑/边界/条件错误；相对旧行为的变化；回归风险 |
| 2 | 并发与生命周期 | 竞态、过期回调、重入、拆除顺序、所有权转移 |
| 3 | 错误处理与边界 | 缺失错误路径、吞错、超时/重试、空/零/最大输入 |
| 4 | 契约与兼容性 | API/格式变更、破坏性变更、默认值、兼容性 |
| 5 | 安全与资源 | 注入、授权缺口、不安全输入、泄漏、无界增长 |

### 1.2 记录（**交给内核，不手工归并**）

把本轮原始输出通过 stdin 交给内核。内核负责归一化、轮内去重、跨轮 id 复用、回归检测：

```bash
cat <<'JSON' | node scripts/loop-state.mjs record .ad-review-loop/<对象标识>-state.json
{
  "round": 1,
  "receipts": [
    { "role": "reviewer", "agentId": "agent-7f2", "provider": "spawn", "fresh": true, "lens": "correctness" }
  ],
  "findings": [
    { "lens": "correctness", "severity": "P1", "location": "src/a.ts:10-20",
      "confidence": 92, "triggerPath": "并发写入未加锁", "evidence": "src/a.ts:14" }
  ]
}
JSON
```

**不要手工做去重或 id 分配**——那正是确定性该覆盖的部分。内核的丢弃与合并全部留痕在 `state.dropped` 里，报告需呈现。

### 1.3 收敛判定（**唯一出口**）

```bash
node scripts/loop-state.mjs judge .ad-review-loop/<对象标识>-state.json
```

内核按固定优先级返回四种判定之一（退出码 1 表示 escalate / hard-stop）：

| 判定 | 触发条件 |
|---|---|
| `converge` | 本轮无阻塞性发现（全部为 nitpick / 理论风险 / 空） |
| `escalate` | 存在未裁决争议项 / 检测到回归 / 连续 2 轮未解决 / 修复引入同级或更严重的新问题 |
| `hard-stop` | 子代理预算耗尽 / 达到 `maxRounds` 仍未收敛 |
| `continue` | 仍有阻塞性发现且未触及其余条件 |

**编排者不得覆盖内核判定。** 若认为判定有误，是修改内核输入（发现数据），不是绕过它。

另外三条**内核无法覆盖、需人工判断**的升级条件：

- 发现需要跨文件架构级改动
- 问题涉及业务语义，"正确行为"无法从代码或文档判定
- 修复触及基线范围外的大量文件

### 1.4 修复（fresh，可写，最小改动）

1. 派生 fresh 修复者（**与审查者不同角色、不同权限**），按文件邻近度分组（同区域 1 个；独立文件可并行；复杂问题减并行、串行以便逐个验证）。
2. 修复者收到：要修的发现清单（含位置与触发路径）+ 相关文件。**不收到审查者的完整推理，也不收到倾向性描述**——只给事实。
3. 约束：
   - **只做最小化、针对性修改**。不加功能、不重构、不"顺手优化"、不修 nitpick。
   - 修复后若项目有 lint/test，运行；失败需先处理再返回。
   - **绝不 commit / push。**
4. 修复者返回每条发现的结果：`fixed（位置）` / `unfixed（原因）` / `false-positive（理由）`。**这是待验证主张。**

### 1.5 验证修复（fresh，只读）并结算

1. 派生 fresh 只读验证者，逐条判定：**该发现是否真的被处理**。
2. 内核的默认即不对称：**先 `resolve --fix`，`--verify` 缺省时若 `fix=fixed` 会自动落到 `unfixed`**。必须显式传 `--verify fixed` 才算通过。

```bash
node scripts/loop-state.mjs resolve .ad-review-loop/<对象标识>-state.json \
  --id F1 --fix fixed --verify fixed --note "已在 src/a.ts:14 加锁"
```

3. 判定三态：`fixed` / `unfixed` / `disputed`。**证据含糊一律 `unfixed`。**
4. 回到 1.1。

### 内核不可用时的降级

若宿主无法运行 `node`，手工维护符合 `state` schema 的 JSON，并在报告的「宿主能力」一节**显式声明降级**：`收敛判定为手工执行，未经确定性复核`。此时把 `maxRounds` 降到 2，并放弃"自动判定"的可靠性声明。

---

## 第 2 步：产出记录

写为工作区中的 Markdown 文件：`.ad-review-loop/<对象标识>-<YYYYMMDD>.md`。

```markdown
# 对抗式审查循环记录 — <对象>

## 1. 范围与基线
- 对象 / 基线分支 / 基线 commit / 最终状态

## 2. 宿主能力与预算
- 派生能力：<机制 / 命令>
- 只读强制：<白名单 / 沙箱 / 仅提示词>
- 可写子代理：<是 / 否 → 只报告模式>
- 确定性内核：<已使用 / 手工降级>
- 预算：maxRounds=<n>，实际轮次=<n>，总子代理=<n>

## 3. 派生凭据
| 轮次 | 角色 | agentId | provider | fresh | 视角 |
|---|---|---|---|---|---|

## 4. 逐轮记录（每轮一节）
### 第 <k> 轮
- 发现：| # | 发现 | 位置 | 触发路径 | 证据 | 严重度 | 置信度 |
- 修复：| 发现 # | 结果 | 位置 | 说明 |
- 验证：| 发现 # | 判定 | 依据 |
- 丢弃/合并：| 原因 | 内容 |  （来自 state.dropped）

## 5. 收敛判定
- 内核判定：<converge / escalate / hard-stop>，理由原文
- 终止原因与依据
- 若未收敛：还剩下什么，为什么没收敛

## 6. 最终状态
| # | 发现 | 位置 | 严重度 | 最终状态 | 首次出现轮次 | 是否回归 |

## 7. 未验证项与范围边界
- 依赖运行验证的结论 / 未做什么 / 未覆盖模块

## 8. 裁决摘要
- 结论 + 建议下一步
```

---

## 状态文件

`.ad-review-loop/<对象标识>-state.json`，**每轮重读、每轮覆写**，由内核维护，请勿手工编辑（除降级场景）。

```jsonc
{
  "version": 1,
  "target": "feat/x",
  "baseline": "a1b2c3d",
  "budget": { "maxRounds": 3, "maxSubagents": 15, "subagentsUsed": 5 },
  "rules": { "minConfidence": 70, "dedupeLines": 3 },
  "findings": {
    "F1": {
      "id": "F1", "lens": "correctness", "severity": "P1",
      "loc": { "file": "src/a.ts", "start": 10, "end": 20 },
      "location": "src/a.ts:10-20",
      "firstSeenRound": 1, "lastSeenRound": 2, "seenRounds": [1, 2],
      "fix": "fixed", "verify": "fixed", "regressed": false, "note": ""
    }
  },
  "rounds": [
    { "round": 1, "findingIds": ["F1"], "receipts": [ /* 派生凭据 */ ] }
  ],
  "dropped": [ /* 每条丢弃与合并都留痕 */ ]
}
```

**为什么必须落盘**：循环会持续多轮，编排者上下文随时可能被压缩或截断。一旦压缩，"这个问题上轮修过吗"就变成猜测，收敛判定随之静默失效。落盘是唯一的宿主通用解药。

---

## 宿主适配

| 宿主 | 派生 fresh 子代理 | 只读强制 | 可写修复者 | 每角色模型 |
|---|---|---|---|---|
| **Claude Code** | `Agent` 工具（`subagent_type`）。**禁用 `/subtask`、`/fork`** | 审查者 agent frontmatter `tools` 白名单 + `PreToolUse` hook 拦写操作 | `general-purpose` 类 agent | `Agent` 工具 `model` 参数 / frontmatter `model` |
| **Codex** | 必须**显式下令**（"spawn N agents"）——Codex 不会自动拆 | 审查者 agent TOML `sandbox_mode = "read-only"` | 单独的 agent TOML，不加只读 | agent TOML `model` + `model_reasoning_effort` |
| **DSH** | `subagent` 工具，`provider: 'spawn'`。**禁用 `fork` provider** | 审查者 `toolFilter: { deny: [...] }` | 修复者放宽 `toolFilter`，但仍 deny 破坏性命令 | `agentOptions.model`（**需回读 `request/header` 自检**） |
| **其他宿主** | 按 0.1 探测，无 fresh 派生能力则**拒绝执行** | 按 0.1 探测 | 按 0.1 探测 | 按 0.1 探测 |

**通用坑（务必核实）**：

- **运行时权限会盖过静态配置。** Codex 中会话里临时改的 `/permissions`、`--yolo` 会**重新套用到派生子代理**，覆盖 agent TOML 的 `sandbox_mode`。派生前先确认当前会话运行时权限。
- **DSH 委托子代理的审批策略固定 `never`**。修复者拿到写权限就是**无审批写入**——所以修复者的 `toolFilter` 必须 deny 破坏性命令（`rm -rf`、`git reset --hard`、`git push` 等），并把修复范围限制在 diff 相关文件。
- **并发是拒绝不是排队。** Claude Code 200/20；DSH 每 owner 10 且满容量直接失败。扇出前先按 0.2 算峰值并发。
- **嵌套默认禁止。** Claude Code 需 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`；DSH 用 `maxDepth`。本 skill 是**扁平**的——编排者派生，子代理不再派生。若发现子代理在派生子代理，说明拆分错了。
- **不要在同一个 owner 上并行跑两个 loop**（会撞 DSH 的并发准入）。
- **语言触发**：`description` 建议中英双语，否则英文宿主上触发可靠性下降。
- **状态文件不要提交**（已在 `.gitignore` 中排除）。

---

## 严重度定义

- **P0** — 真实路径上阻塞、数据损坏、崩溃或安全问题。
- **P1** — 明确缺陷，存在绕行方案。
- **P2** — 边界/健壮性问题。
- **nitpick** — 风格偏好；**不参与修复**，仅记录，不影响"干净"结论。
- **理论风险** — 未找到可执行路径；证据出现前不打 P0/P1/P2。

未知严重度由内核保守归一为 `P2`（仍阻塞收敛）。

---

## 反模式清单（明确禁止）

- ❌ 编排者自己扮演审查者或修复者（违反不变量 1、2）
- ❌ 复用上一轮的审查者（fresh 是特性）
- ❌ 把上一轮结论告知本轮的 fresh 审查者
- ❌ 让审查者顺手修复，或让修复者审查自己的改动
- ❌ 依赖对话记忆做收敛判定，不读状态文件
- ❌ **覆盖或绕过内核的 `judge` 判定**
- ❌ 手工做去重/id 分配（那正是确定性该覆盖的部分）
- ❌ 无硬轮次上限、指望"自然收敛"
- ❌ 把争议项当共识通过
- ❌ 修复时加功能、重构、"顺手优化"
- ❌ commit / push / 改历史
- ❌ 为了"跑完"而在子代理失败时跳过它并假装覆盖完整

---

## 验证清单

- [ ] 每个子代理都有派生凭据（`role`/`agentId`/`provider`/`fresh`）；无凭据者不计入覆盖
- [ ] 每轮审查者都是 fresh，且未被告知历史结论
- [ ] 审查者与修复者是不同角色、不同权限
- [ ] 状态文件每轮重读 + 覆写；收敛判定引用了内核输出的原文
- [ ] 峰值并发未超宿主上限（已按 0.2 核算）
- [ ] 每条 P0/P1/P2 都有证据与触发路径；无位置的发现已在 `dropped` 中留痕
- [ ] 每条修复都有独立验证结论；含糊项判为 `unfixed`
- [ ] 终止原因明确（`converge` / `hard-stop` / `escalate`）
- [ ] 未发生 commit / push；基线范围外文件未被修改
- [ ] 报告含宿主能力、派生凭据、丢弃留痕、未收敛说明、范围边界五节
